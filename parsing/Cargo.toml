[package]
name = "win-security-identifier-parsing"
description = "Core parsing processing for the win-security-identifier project do not use it directly use win-security-identifier instead"
version.workspace = true
authors.workspace = true
license.workspace = true
edition.workspace = true
rust-version.workspace = true
repository.workspace = true
keywords = ["windows", "sid", "security-identifier", "acl", "sddl"]
categories = ["os::windows-apis", "parser-implementations", "data-structures"]

[features]
default = ["std"]
std = ["thiserror/std"]

[dependencies]
arrayvec = { workspace = true }
thiserror = { workspace = true }

[dev-dependencies]
proptest = "1.7.0"

[lints]
workspace = true
