# win-security-identifier-macro
Proc macro for the `win-security-identifier` project
The `Sid!` macro provide a way to parse sid to a `ConstSid<N>` with `N` determined automatically. Do not use it directly instead enable the `macro` feature of the main project